console.log("Challenge1");

let triangle_count = prompt("Enter the triangle size: ", [0])

for (let index1 = 1; index1 <= triangle_count; index1++) {
    for (let index2 = 0; index2 < index1; index2++) {
        console.log(index1);
    }
}
